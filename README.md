# Online Courses Catalog

Каталог онлайн-курсов с живым поиском и фильтрацией по категориям.  
Выполнено в рамках тестового задания: pixel-perfect вёрстка по макету Figma, резиновый адаптив, чистый код на vanilla HTML + SCSS + JavaScript.

**Живая демонстрация**: [https://our-online-courses.vercel.app](https://our-online-courses.vercel.app)

## Особенности проекта

- **Pixel-perfect** вёрстка по макету Figma (1920px → 320px)
- **Резиновый адаптив** без жёстких media queries — плавное масштабирование с помощью `clamp()`, `minmax()`, flex и grid
- **Живой поиск** по названию курса (мгновенная фильтрация при вводе)
- **Фильтрация по категориям** с комбинированием (поиск работает внутри выбранной категории)
- **Плавные анимации** появления/исчезновения карточек
- **Динамические счетчики** в фильтрах
- Чистый код по **BEM-методологии**
- Только **vanilla JS** — без библиотек и фреймворков
- Шрифты: **Lato** (основной), fallback на Inter
- Декоративные векторные элементы на CSS (жёлтые абстрактные фигуры, как в макете)

## Технологии

- HTML5
- SCSS (компилируется в CSS)
- CSS Math Functions - calc(), min(), max()
- Vanilla JavaScript
- Google Fonts (Lato + Inter)

## Структура проекта

```
project/
├── index.html          # Основная разметка
├── variables.css       # CSS-переменные
├── styles.css          # Основные стили
├── script.js           # JavaScript логика
└── images/             # Изображения курсов
```

## Ключевые CSS-техники

### Резиновые вычисления
```css
/* Пример плавного масштабирования */
font-size: clamp(
  14px,
  calc(14px + (16 - 14) * ((100vw - 320px) / (1920 - 320))),
  16px
);
```

### Адаптивные карточки
```css
/* Автоматическое изменение высоты */
aspect-ratio: 390 / 438;
height: auto;

### Современные селекторы
```css
/* Центрирование 1-2 карточек */
.catalog__grid:has(.catalog__card:nth-last-child(1):nth-child(1)) {
  justify-content: center;
}
```

## Как запустить локально

1. Склонируй репозиторий:
   ```bash
   git clone https://github.com/Alex-Ven/Our-online-courses
   ```
2. Открой `index.html` в браузере (не требуется сервер — всё статично)

## Деплой

Проект развёрнут на **Vercel**:  
[https://our-online-courses.vercel.app](https://our-online-courses.vercel.app)

Автоматический деплой при каждом push в main.

## Что можно улучшить / доделать

Проект практически соответствует ТЗ, но вот идеи для дальнейшего развития:

### Функционал
- Реализовать кнопку **"Load more"** с подгрузкой новых курсов (из JSON или mock-данных)
- Добавить **пагинацию** или infinite scroll
- Сделать **сортировку** по цене, популярности, алфавиту
- Добавить модальное окно с деталями курса при клике на карточку

### Дизайн / UX
- Добавить hover-эффекты на бейджи и цену
- Сделать тёмную тему (переключатель уже есть в макете — можно реализовать)
- Улучшить доступность (ARIA-атрибуты, фокус для клавиатуры)

### Технически
- Перейти на сборку через Webpack/Vite для удобной разработки с SCSS
- Вынести данные курсов в отдельный JSON-файл
- Добавить прелоадер при фильтрации (если данных будет много)

### Оптимизация
- Сжать изображения (WebP)
- Добавить lazy-loading для фото ниже складки

### API-интеграция
   ```javascript
   // Загрузка курсов с сервера
   async function loadCourses(page = 1) {
     const response = await fetch(`/api/courses?page=${page}`);
     return response.json();
   }
   ```

---

Проект выполнен с душой и вниманием к деталям. Готов к ревью и доработкам по обратной связи!

Спасибо за интересное тестовое задание 😊

*"Обучение - это не подготовка к жизни, это сама жизнь"*

**Автор**: [Alexander Venedyukhin]  — Aspiring Frontend / Full Stack Developer
 
[LinkedIn](https://linkedin.com/in/alexander-venedyukhin-1288abb2) | [GitHub](https://github.com/Alex-Ven)
